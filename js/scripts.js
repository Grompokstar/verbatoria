function toggleVisibilityMainMenu() {
    $('.main-menu').toggleClass('visible');
    $('.main-menu-btn').toggleClass('close');
    $('.main-menu-btn svg').toggleClass('visible');
    $('.overlay').toggleClass('visible');
}